package mn.innex.stay.review.domain;

/** Who a review is about. */
public enum ReviewSubject {

    /** The guest reviewing the place they stayed in. */
    SUPPLY,

    /** The host reviewing the person who stayed. */
    GUEST
}
