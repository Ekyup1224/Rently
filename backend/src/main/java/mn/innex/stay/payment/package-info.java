/**
 * QPay, SocialPay and Stripe integrations, plus payouts and commission application.
 * Filled in during Step 2 (checkout), extended in Step 4 (payout oversight).
 *
 * <p>Module boundary per build-spec section 3: other modules depend on this
 * package only through its published service interfaces, never on its entities.
 */
package mn.innex.stay.payment;
