/**
 * Polymorphic bookings over both supply types (PROPERTY | HOTEL), pricing breakdown and
 * cancellation policy evaluation. Filled in during Step 2, extended in Step 3.
 *
 * <p>Module boundary per build-spec section 3: other modules depend on this
 * package only through its published service interfaces, never on its entities.
 */
package mn.innex.stay.booking;
